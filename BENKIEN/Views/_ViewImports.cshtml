@using BENKIEN
@using BENKIEN.Models
@using BENKIEN.Entities

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!-- Diğer HTML ve Razor kodları -->
@if (Model != null)
{
    <!-- Model'den gelen verileri kullan -->
    <!-- Örneğin: @Model.Sliders, @Model.Products, @Model.ProductImages @Model.Cart @Model.TopHeader
        vb. -->
}
